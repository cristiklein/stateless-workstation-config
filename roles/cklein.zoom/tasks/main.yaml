---
- name: Download zoom.deb
  ansible.builtin.get_url:
    url: https://zoom.us/client/latest/zoom_amd64.deb
    dest: /tmp/zoom.deb
    mode: '0644'

- name: Install Zoom
  become: true
  ansible.builtin.apt:
    deb: /tmp/zoom.deb
